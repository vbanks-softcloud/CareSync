{
  "name": "@caresync/db-scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "One-off scripts for applying schema, seeding data, etc. Not part of the deployed app.",
  "scripts": {
    "apply-schema": "node apply-schema.mjs"
  },
  "dependencies": {
    "mysql2": "^3.11.5"
  }
}
